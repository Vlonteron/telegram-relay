{
  "name": "telegram-relay",
  "version": "1.0.0",
  "description": "HTTP relay for Telegram Bot API (for servers without direct access to api.telegram.org)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
